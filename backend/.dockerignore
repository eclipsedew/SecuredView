venv/
__pycache__/
*.pyc
.env
.app_key
.jwt_secret
.admin_creds
*.db
*.db-*
securevpn.db*
.git
.github
# Account snapshot (PIN hashes) must never ship inside the image — prod
# seeds from the SEED_DATA env var only.
seed.json
